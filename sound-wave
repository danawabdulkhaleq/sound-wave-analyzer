<!DOCTYPE html>
<html lang="ar">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>محلل الموجات الصوتية للمواقع التاريخية</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            direction: rtl;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            text-align: center;
        }
        header {
            background-color: #4CAF50;
            color: white;
            padding: 20px;
            font-size: 2em;
        }
        section {
            margin: 20px;
        }
        h2 {
            color: #333;
        }
        canvas {
            border: 1px solid black;
            margin-top: 20px;
        }
        footer {
            background-color: #333;
            color: white;
            padding: 10px;
            position: fixed;
            bottom: 0;
            width: 100%;
        }
    </style>
</head>
<body>

<header>
    محلل الموجات الصوتية للمواقع التاريخية
</header>

<section>
    <h2>كيف يعمل المشروع</h2>
    <p>
        هذا المشروع يستخدم تقنيات تحليل الموجات الصوتية لفحص التفاعلات الصوتية في المواقع التاريخية في مصر. يمكنك إدخال البيانات المتعلقة بالهيكل،
        مثل الأبعاد والمواد المستخدمة، لتحليل كيفية انتشار الموجات الصوتية واكتشاف الأنماط الصوتية الفريدة.
    </p>
    <p>انقر على الزر أدناه لرؤية المحاكاة.</p>
    <button onclick="runSimulation()">تشغيل المحاكاة</button>
</section>

<section>
    <h2>المحاكاة</h2>
    <canvas id="waveCanvas" width="600" height="400"></canvas>
</section>

<footer>
    مشروع فريد لتحليل المواقع الأثرية باستخدام الفيزياء والبرمجيات
</footer>

<script>
    function runSimulation() {
        const canvas = document.getElementById('waveCanvas');
        const ctx = canvas.getContext('2d');

        let wavePosition = 0;
        const waveSpeed = 2;

        function drawWave() {
            ctx.clearRect(0, 0, canvas.width, canvas.height);

            // رسم الجدران الجانبية للهيكل
            ctx.fillStyle = '#888';
            ctx.fillRect(50, 50, 20, 300); // الجدار الأيسر
            ctx.fillRect(530, 50, 20, 300); // الجدار الأيمن

            // رسم الموجة الصوتية
            ctx.beginPath();
            ctx.moveTo(50, canvas.height / 2);
            for (let x = 50; x < canvas.width - 50; x++) {
                const y = 200 + Math.sin((x + wavePosition) * 0.05) * 50;
                ctx.lineTo(x, y);
            }
            ctx.strokeStyle = '#4CAF50';
            ctx.lineWidth = 3;
            ctx.stroke();

            wavePosition += waveSpeed;

            // استمرار تشغيل المحاكاة
            requestAnimationFrame(drawWave);
        }

        drawWave();
    }
</script>

</body>
</html>
